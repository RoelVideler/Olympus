# Iaso: Health, symptoms, vitals, Withings sync
model:
  provider: openai-compatible
  base_url: http://localhost:11434/v1
  model: qwen3.6-8b

run_mode: on-demand

toolsets:
  - web
  - memory
  - share_knowledge

agent:
  max_turns: 15
